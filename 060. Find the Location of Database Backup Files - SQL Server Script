-- 060. Find the Location of Database Backup Files - SQL Server Script

-- Useful when another database developer or DBA is not available or don't know where is the backup file of database.

SELECT Distinct physical_device_name 
FROM msdb.dbo.backupmediafamily
